# to open container's terminal :
docker exec -it <container-name or id> bash

docker compose up --build # add -d to detach it form the terminal
docker compose down

docker exec -it kali-attacker bash
docker exec -it metasploitable2 bash
